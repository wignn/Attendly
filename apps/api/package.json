{
  "name": "@komas/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "go build -v -o bin/server cmd/server/main.go",
    "dev": "go run cmd/server/main.go",
    "test": "go test -v ./...",
    "lint": "command -v golangci-lint >/dev/null 2>&1 && golangci-lint run ./... || go vet ./...",
    "typecheck": "go vet ./...",
    "clean": "rm -rf bin"
  }
}
